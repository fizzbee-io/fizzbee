// Code generated by fizzbee-mbt generator; DO NOT EDIT.

package {{ package_name }}

import (
    "github.com/fizzbee-io/fizzbee/mbt/lib/go"
)

// Role interfaces
{% for role in file.roles %}
type {{ role.name }}Role interface {
    mbt.Role
    {%- for action in role.actions %}
    {%- if action.name != "Init" %}
    Action{{ action.name }}(choices []mbt.Choice) (any, error)
    {%- endif %}
    {%- endfor %}
}
{% endfor %}

// Model interface for the whole specification
type {{ model_name }}Model interface {
    Init() error
    Cleanup() error
    {%- for action in file.actions %}
    {%- if action.name != "Init" %}
    Action{{ action.name }}(choices []mbt.Choice) (any, error)
    {%- endif %}
    {%- endfor %}
}
