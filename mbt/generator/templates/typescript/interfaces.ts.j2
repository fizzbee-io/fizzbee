// Code generated by fizzbee-mbt generator; DO NOT EDIT.
// Source: {{ source_path }}

import { Role, Model, Arg } from '@fizzbee/mbt';

// Role interfaces
{% for role in file.roles %}
export interface {{ role.name }}Role extends Role {
  {%- for action in role.actions %}
  {%- if action.name != "Init" %}
  action{{ action.name }}(args: Arg[]): Promise<any>;
  {%- endif %}
  {%- endfor %}
}
{% endfor %}
// Model interface for the whole specification
export interface {{ model_name }}Model extends Model {
  // Inherits from Model: init(), cleanup(), getRoles()
  {%- for action in file.actions %}
  {%- if action.name != "Init" %}
  action{{ action.name }}(args: Arg[]): Promise<any>;
  {%- endif %}
  {%- endfor %}
}
