# Realistic scale — use with simulation mode only
# Symmetry domain limits are unchanged; simulation mode ignores them anyway.
# Usage: ./fizz -x --max_runs 1 --seed 42 --preinit-hook-file config_large.cfg SalonSchedule.fizz
DAYS_IN_WEEK = 7
SLOTS_PER_DAY = 12
BOOKING_WINDOW = 7
NUM_CUSTOMERS = 10
