# Single customer, wider schedule — verifies clock/cleanup with more days
# Usage: ./fizz --preinit-hook-file config_multiday.cfg SalonSchedule.fizz
NUM_CUSTOMERS = 1
DAYS_IN_WEEK = 3
SLOTS_PER_DAY = 1
